# 更新记录

## v4.6.1（2026-09-23）

**发布后修正：让 GitHub 正确识别 MIT 许可证。** 仓库推上去之后 GitHub 把许可证标成
"Other"：`LICENSE` 正文是标准 MIT，但后面还挂了一段中文出行安全提示，许可证识别器
就把整份文件判成了非标准文本。公开仓库里这个标签直接影响别人判断「能不能用」，所以改掉。

- `LICENSE` 恢复为纯 MIT 原文，著作权人仍是项目名 `travel-orchestrator`
- 原先挂在 `LICENSE` 末尾的出行安全提示**逐字移到 README 新增的「出行安全」一节**，
  内容一处没少，位置反而更显眼（README 是仓库首页，`LICENSE` 很少有人点开）
- 版本号 4.6.0 → 4.6.1（`SKILL.md` frontmatter 与页脚同步），功能与文案未动

## v4.6.0（2026-09-23）

**优化生成环节：数据以分片为常态，不再落中间大文件。** 症状是「生成 MD/HTML 很慢、而且很容易
遇到问题」，追下去发现两件事都不是渲染慢（渲染 15 KB 数据实测 0.04 秒）：一是行程数据要求
**一次写完** 20-40 KB 的 `trip.json`，写坏一次只能临时手写脚本拼，既慢又不可复用；二是渲染器
对坏数据太宽容：字段缺了只渲染成空白，命令照样返回 0，看着成功、交付出去才发现是废纸。

- **数据固定在 `parts/*.json`**：`build_trip.py` v1.0.0 → v1.1.0 新增 `render`（合并 + 校验 +
  渲染一条龙）与 `split`（把已有大 JSON 拆成分片，**拆完自动校验合并结果与原文完全等值**），
  `validate` 目录 / 文件都吃，`assemble` 降级为「确实需要一份独立大 JSON（交付/存档）时才用」。
  渲染器、红方复核、校验统一走 `build_trip.resolve_trip()`：给目录就地合并，给文件直接读，
  于是「组装写坏」与「产物和分片不同步」这两个故障源一起消失。合并结果按固定顶层键序重排，
  `assemble` 出来的 JSON 稳定、好 diff
- **渲染器补结构门**：`render_plan.py` v3.3.0 → v3.4.0 在写文件前先按 build_trip 的规则校验结构
  （缺字段 / `item_id` 重复 / 预算 min>max 等），不通过抛 `TripDataError`、CLI 退出码 **3**，
  且不留下半成品产物；`--force` / `force=True` 只留给排障。模板、九节章节、样式一字未动
- **示例改成真分片**：`examples/weihai-2d/` 的 `trip.json` 拆成 13 片
  （`01_request` … `27_decisions`，最大一片 3.4 KB），目录里不再留中间大文件。
  示例里分片是**平铺**在方案目录里的，不套 `parts/`：上架包限制目录不超过两层，
  `examples/weihai-2d/parts/x.json` 会多出一层。`build_trip.resolve_trip()` 两种形态都认
  （分片多时收进 `parts/` 更整齐，`split` 就默认写那里）
- 新增**用例 9 · 分片数据流**（拆合等值、目录/文件同源、结构门拦截、复核器吃目录）
- **验收不再写脏仓库**：早先 `tools/verify_all.py` 的渲染那步是*就地*重渲染 `examples/`，
  而产物头部带「生成于 <时间>」，于是每跑一次验收，仓库里的示例产物、安装目录、上架 zip
  三方立刻互相不一致，每次提交都多一条无意义的 diff（就为那一行时间）。现在渲染出到临时
  目录，用得着的产物一个字节不动；示例产物有没有跟上渲染器，交给新增的第 6 步判定
  （忽略生成时间后逐字节比），要刷新就显式加 `--refresh`
- **打包产物不再落进源码目录**：`tools/pack.py` 的默认输出原先算成了 `<源码>/.workbuddy/dist/`，
  于是源码树里莫名冒出一个 `.workbuddy/`、包却不在工作区。改为 `<工作区>/.workbuddy/dist/`
- 新增**用例 10 · 工具链不写脏仓库**看住上面两点，断言 129 → **133 条**
- 顺手修掉一个隐患：`red_team.py` v1.1.0 → v1.2.0 原先只认文件路径，现在同样支持目录

## v4.5.2（2026-09-23）

**把大同那轮暴露的三个问题真正修进代码。** v4.3.3 曾经修过前两个，但那次的改动只落在
安装目录（`~/.workbuddy/skills/travel-orchestrator/`），后来从源码同步时被覆盖，问题
一直留在源码里（大同交付物里能看到修好的文案，源码却还是旧版）。这次改进源码，并加了一组
断言看住它们。

- **重名地名会静默算错坐标**（P0）：`--city 大同` 解析到黑龙江大同（46.04N 124.81E），
  日出比山西大同早 46 分钟。`nature_forecast.py` v2.3.0 地理编码改 `count=5`，同名候选
  跨省时在 stderr 与输出正文都打 `[注意]`，列清本次用的是哪个、还有哪些候选
  （取数仍取第一条，选择权留给人）。`references/nature-apis.md` 补 §6.2 完整案例与四条规则，
  SKILL.md 注意事项加一条硬规则
- **MD「使用须知」硬编码「赶海潮汐」**：没有赶海的方案开头却让人复核潮汐表。改为按
  `nature` 里实际有的专项拼句子，没有的专项不出现
- **鸟种卡的降级胶囊被长句撑坏**：`species[]` 写成一整句话时，整句变成一个胶囊、外链
  还拿整句去搜图鉴。`nature_widgets.py` v1.1.0 按首个标点拆成「鸟名 + 说明」，名字位
  只留鸟名（说明进小灰字，完整文本在 title 里），外链只拿鸟名做关键字；SKILL.md 写明
  纯字符串只写鸟名
- 新增**用例 8 · 回归看门**，断言 112 条（原 91 条）
- **全库行尾统一为 LF**：原来 README / SKILL / CHANGELOG / USAGE / tests / render_plan
  等 14 个文件是 CRLF，而交付物本来就是 LF。改一处不再整篇重排，补丁脚本也不用再猜
  每个文件的换行符；新增 `.gitattributes` 固定 `eol=lf`，`.gitattributes` 不进安装目录

## v4.5.1（2026-09-23）

**上架前收尾。** 只动署名、上架元数据与断言，功能、文案、接口都没改。

- `LICENSE` 的著作权人由 `travel-orchestrator contributors` 改为项目名 `travel-orchestrator`，
  与交付物页脚署名（`由 旅行规划总编排（travel-orchestrator）生成`）口径一致
- `SKILL.md` frontmatter 补齐开放平台必填项：除已有 `description` 外新增 `description_zh`、
  `description_en`、`version`、`author`，另加 `display_name`、`display_name_en` 两行；
  `author` 署项目名 `travel-orchestrator`，与 `LICENSE` 同口径
- 回归测试新增**用例 7 · 上架元数据**：14 条防漂移断言（frontmatter 必填项、`author` 与
  `LICENSE` 同口径、中英简介与 `workbuddy.json` 同步、版本号与页脚一致），77 → 91 条
- `README.md` 新增「上架到 SkillHub」一节：必填字段表、自检与打包命令、上传入口提醒
- 页脚版本号同步为 v4.5.1
- 示例样本暂缓补入：先按用户决定把 skill 放进真实使用跑一段时间，再用真实行程回填 `examples/`

## v4.5.0（2026-09-23）

**文档与代码对齐。** 这一版没动功能，改的是「文档里写的」和「代码实际做的」对不上的地方，
顺手把全库文案按 `references/writing-style.md` 过了一遍。

- **数字口径**：测试断言统一为 **77 条**（README 原来写 24、USAGE 原来写 69）；
  脚本版本号补正：`render_plan.py` v3.1.0 → v3.2.0、`red_team.py` v1.0.0 → v1.1.0、
  `nature_widgets.py` v1.0.0 → v1.0.1
- **赶海窗口口径统一**为**干潮前后 1.5 小时**（与 `nature_forecast.py` 的算法一致，
  `SKILL.md` 原来有两处写 2 小时）
- **自然专项统一为四项**（观鸟 / 观星 / 日出 / 赶海），火烧云评分并入日出日落卡：
  README 不再单列「朝晚霞」一行，`SKILL.md` 的「专项五」改为「专项四的补充」
- **`USAGE.md` 修掉三处错**：JSON 示例里 `decisions` 写了两遍，后一条把带注释的版本盖掉；
  目标天体字段路径写成 `tour.stargazing.targets[]`（应为 `stargazing.targets[]`）；
  「红方结论渲染时会成为交付物的一节」与「复核不进交付物」自相矛盾，改为写回 `red_team` 供 S10 回看
- **`SKILL.md` 接口清单**去掉已下线的 SunsetBot 与旧潮汐源，改为 Open-Meteo Marine
- **文案**：全库正文清掉中文破折号 221 处（代码块与行内代码里的保留），
  中英混排空格补齐（`B站` → `B 站`、`5月` → `5 月`、`AI旅行规划师` → `AI 旅行规划师`）
- 新增 `workbuddy.json`（展示名 + 中英简介），供上架市场用

## v4.4.0（2026-09-23）

**S1 从"一次抛一串问题"改成 grill-me 式的需求访谈**，自然观察专项嵌进访谈里。

原来 S1 是一条消息问六件事，用户只会挑最容易答的两个答，剩下的留白，Agent 就按默认值补：
最后方案看着完整，其实是 Agent 的偏好披了用户的壳。现在改成一次一问、逐层往下挖。

- **新增 `references/intake-interview.md`**（S1 执行手册）：四条硬规则（一次一问 / 每题带推荐项 /
  **能自己查到的事实不拿去问用户** / 共识确认前不写方案）、五层决策树
  （L0 意图 → L1 硬约束 → L2 玩法口味 → L3 自然观察专项 → L4 风险底线）、
  追问对照表（"随便玩玩"该怎么追、"预算 3000 够吗"该怎么追问口径）、
  收尾「需求共识」模板与落盘字段映射、反例清单
- **自然观察嵌进访谈 L3**：拿到人和日期后**先跑可行性核查**（`nature_forecast.py`），
  再按三档如实告诉用户：条件好 → 问做不做；条件一般（月照 75%、潮位一般）→ 说清代价再问
  值不值得专门排；**做不了 → 直说并给替代**。不许为了"满足用户"硬排一个玩不到的项目。
  观鸟/观星/日出/赶海各 2-4 个深挖问题（装备、经验、能不能 4:30 出门、能不能蹲 2 小时）
- **访谈结果落盘**：`request` 新增 `why` / `constraints[]` / `taste` / `risk` / `nature_caps`；
  `decisions[]` 记每个被问到的决定（`question`/`choice`/`reason`，被顶回过仍坚持的加 `pushed_back`）
- **问询记录不进交付物**（与红方复核同理，属过程记录）：交付物只留共识的结论：
  `request.why` → 速览「这趟的主题」、`request.constraints[]` → 速览「硬约束」，
  长句整行通栏（`.grid .cell.wide`）。测试里有反向断言，谁把问询记录渲染回产物都会挂
- `render_plan.py` v3.1.0 → v3.2.0；示例 trip.json 补上真实的访谈口径（8 条 decisions，
  含两条"提醒过风险、用户仍坚持"的记录）
- **速览重排（规整化）**：原来「必去专项 / 优先专项」两格长得一样，读者分不出区别：
  并成一格「专项偏好」，口径写进值里（`必须玩到：赶海、观鸟　·　能做则做：观星、日出`）；
  「目的地 + 出发地」并成一格「往返」（`北京 → 威海`）。
  速览改用**定死列数的网格**（`.g-glance`，4 列 / 2 列 / 1 列三档）：原来 `.grid` 是 auto-fit
  自动列数，"占两列"在不同宽度下含义不一样，压成两列必然歪。现在格子按 span 拼、行行填满：
  4 列时「专项偏好 + 硬约束」各占两列并排，窄屏各自整行

回归断言 41 → 77 条。
## v4.3.2（2026-09-22）

四个交互件上线后过了一遍无头浏览器截图，修掉排版上的几处毛病：

- **地点搜索词被剥残**：`search_keyword()` 把「观鸟点 / 观景台」当尾缀剥掉，
  于是「荣成大天鹅国家级自然保护区开放观鸟点」的搜索词变成「…自然保护区开放」，
  高德搜不出东西。现在只剥「备选」这类真正的噪声词，剥完不足 2 字就退回原文。
- **地点串的「、」甩在行尾**：分隔符原本是 HTML 文本节点，排不下时孤零零落在行尾。
  改成 `.pl{display:flex;flex-wrap:wrap}` + `.pl-item+.pl-item::before{content:"、"}`，
  分隔符永远跟着后一项走；`.pl-item` 又是整体换行单元，不会把「地名（备注）」拆开。
- **地点行通栏**：观察点/观测点/地点挤在 auto-fit 的 1/3 列里被拆得七零八落。
  `_nature_card()` 的 rows 支持第 4 位 `wide`，这类行 `grid-column:1/-1`。
- **通栏卡要点改两列**：`.card.tall .dl` 用 `minmax(300px,1fr)`，
  「注意」这种长值不再被挤进 270px 挤出四五行、右边还空一列。
- **星图行的留白**：右列原来写死 140px，星图居中在 1fr 里，正文两侧各空一大片。
  改成 `minmax(0,1.15fr) minmax(0,1fr)`，并把「今晚看什么」挪进右列与月相盘同列。
- **场景标签读不清**：通栏卡把场景从竖条改成顶部横带后，
  观鸟的芦苇丛（left:12/25/38px）正好压在「观鸟」标签下面。
  标签改半透明深色胶囊、芦苇整丛挪到 22% 处让开。
- **打印态**：横带压到 38px，`min-height:118px` 会顶回 `height:38px`
  （必须同时清 `min-height:0`）；标签改成白底描边的印刷小签
  （底色从浅薄荷到深藏蓝都有，纯白字或纯黑字总有一种读不清）；芦苇不画（会被切一半）。

回归断言 35 → 41 条。

---

## v4.3.0（2026-09-22）

自然观察专项从"看数据"变成"能用起来"：鸟种卡、示意星图、天光条 + 霞光指数、潮汐点选图。
新增一个专职模块 `scripts/nature_widgets.py`，交付页仍然是**无脚本、无远程资源的离线单文件**。

### 新增：四个交互件

- **观鸟 · 鸟种卡**（`<details>`，点开展开）
  - 收起时是一行：鸟种剪影 + 中名 + 学名 + 保护等级徽章 + 「查图 · 详情」
  - 展开后给分类、保护等级全量、本地最佳月份、全球记录数、简介与简介来源
  - 外链四枚：图鉴页（iNaturalist taxa 页）、查图片（百度图片搜索）、本地记录、全球地图
  - 剪影按类群自动选（鸥 / 鹬 / 雁鸭 / 猛禽 / 长颈 / 雀形），颜色走六色循环
  - 只写了鸟种名（字符串）也能显示，降级成一行小胶囊 + 两个外链
- **观星 · 示意星图 + 月亮盘**
  - 内置 43 颗亮星（J2000）与 6 组星群连线（北斗、仙后、秋季四边形、夏季大三角、猎户、猎户腰带）
  - 真算：儒略日 → 格林尼治恒星时 → 地方恒星时 → 时角 → 高度/方位，只画地平线以上的星，
    星点大小按星等，标签互相避让，星群名压回圆环内侧
  - 鼠标悬停任一星点显示「高度 / 方位 / 星等」（SVG `<title>`，仍是零脚本）
  - 月亮盘按照度画真实终结线（椭圆半轴 = R·|2k-1|），标明亏凸月亮面在左
  - 图下如实写口径：未做岁差、大气折射、光污染修正，不是精密星历
- **日出日落 · 天光条 + 霞光（火烧云）指数**
  - 天光条把蓝调 / 黄金 / 白昼 / 夜间画成一条横向天色渐变，标出日出与日落刻度，
    窄到放不下字的时段（蓝调只有 25 分钟）靠下方色块图例对位置
  - **霞光从独立一节并进日出日落卡**：0-3 级点阵 + 逐因子理由 + 输入数据（低云/中高云/高云/能见度/湿度）
  - 黄昏侧黄金/蓝调时刻按同一套定义推算，页面上标明"未单独取数"
- **赶海 · 潮汐点选图**
  - 逐时潮位曲线（面积 + 折线）+ 赶海窗口色带 + 满潮/干潮标记
  - **点曲线上的圆点即切换时刻**：下方面板显示该时刻潮位、涨落、能否下滩的建议
  - 实现方式是原生 `<input type="radio">` + CSS 兄弟选择器（`:nth-of-type(k):checked ~`），
    没有用任何脚本；默认选中干潮那一刻
  - 极值（含 06:08 这种非整点）会替换掉最近的小时点，避免两个圆点叠在一起
  - 只画接口真实给到的时段，没数据的时段不补线（威海例子只到 07:00）

### 变更

- 自然专项的卡带图表/长清单时改**通栏布局**（场景动效从左侧竖条挪到顶部 118px 横条），
  图表拿得到全部宽度；不带交互件的卡仍是左竖条
- 地点字段（`hotspots` / `spots` / `best_spot`）支持写对象数组 `[{name, note}]`，
  渲染成可点的高德搜索链接并把备注排成小字；搜索词会自动剥掉「（朝东无遮挡）」这类括号附注
- Markdown 版同步升级：鸟种表（学名/保护等级/最佳月份/图鉴链接）、逐时潮位表、霞光指数与判据

### 修正

- **`.tp:checked ~` 只判断"前面有没有选中的单选框"，不会把第 k 个单选框和第 k 块面板绑起来**：
  32 条选择器全部命中，8 块面板一起显示（已实测确认）。改为
  `.tp:nth-of-type(k):checked ~ .tp-panels > .tp-panel:nth-child(k)`
- 星图与月亮盘左右反了（月亮占了宽列，星图缩成 140px），已对调并给星图限宽 440px
- 星图星群名被推到圆环外的浅色底上，黄字看不清：加了压回圆环内侧的钳制与深色描边晕
- 打印时鸟种卡不展开：Chromium 131+ 用 `::details-content` 的 `content-visibility`
  控制折叠内容，只覆盖子元素 `display` 已经不够，两条都补上
- SVG 里用了 `.card` / `.grid` 当类名，和 HTML 布局类撞车（会给 `<circle>` 套上 `display:grid`），
  改名 `.cardi` / `.skygrid`

### 示例与测试

- `examples/weihai-2d/trip.json` 升到 v3：鸟种卡数据取 iNaturalist 实测返回（黑嘴鸥 VU、
  蛎鹬 NT、大天鹅 LC），潮位曲线 8 个点取 Open-Meteo Marine 实测，霞光评分 2/3 取接口实测，
  新增 facts `f6/f7` 与来源 `s6`。红方复核仍是"通过"，0 阻断 0 严重
- `tests/run_tests.py` 新增用例 5（11 条断言），除查产物痕迹外还直接查算法：
  **北极星的地平高度必须接近观测地纬度（差 <6°）、10 月 1 日晚的心宿二必须已在地平线下**。
  共 35 项断言全绿

## v4.2.0（2026-09-22）

UI/UE 二轮改造：自然专项不再是小格子、背景布灵布灵、逐日行程可交互、全站换圆体与更多颜色。

### 变更（`render_plan.py` v3.0.0 / `assets/handbook.css` v3.0.0）

- **自然观察专项改成"一项一整行"**：左侧 216px 场景动效竖条（右侧剪扇形缺口），
  右侧要点排两列，每条要点带一根彩色细条。窄屏（≤680px）自动转成上图下文。
  原来的 `auto-fit` 宫格在 4-5 个专项时会出现大小不一的空格，已弃用。
- **逐日行程加交互**（全部纯 HTML+CSS，仍无脚本）：
  - 行程章节顶部新增**日期锚点条**，点一下跳到那天
  - 每天变成可折叠的 `<details open>`（默认展开，summary 带日期、标题、条目数徽章）
  - **地点变成可点地图链接**（高德搜索 URL，新窗口打开，页面本身不联网）
  - 时间轴条目悬停淡粉底 + 右移
  - Markdown 版的地点同样带地图链接
- **待办勾选框变成真能点的 `checkbox`**（勾上变蓝底白勾 + 文字划线），不再是 ☐ 字符
- **依据索引的来源网址变成可点链接**；依据表窄列不再折行（表头、编号、状态、取数日期）
- 文档表述统一：交付物是「攻略页」，不再叫「手账」

### 视觉

- **字体全站换圆体栈**：拉丁优先 Varela Round / Quicksand，中文依次尝试
  圆体-简 / 幼圆再回退黑体（离线不能引字库，只能这样尽力命中）。圆体字面方，
  标题取消负字距，改 0 ~ +0.2px。
- **配色扩到六色循环**：粉 / 蓝 / 薄荷 / 紫 / 金 / 橙，九个章节图标各不同色，
  标签、要点条、日期徽章跟着分色，不再一路粉到底。
- **背景布灵布灵（三层 + 星点层）**，全部用渐变画，不引图片：
  ① 浅粉白底 + 斜纹 + 圆点阵列 ② 五团彩色柔光 34s 缓慢漂移 ③ 闪光斜纹 7s 呼吸
  ④ 12 颗 `clip-path` 四角星错峰闪烁。装饰层统一 `z-index:-1`，永远压不到内容。
- **新增 15 枚内联 SVG 小贴图**（贝壳/小鸟/蝴蝶结/相机/奶茶/硬币/心/云/叶/星/花/路锥/地图针…），
  分布在封面（3 枚）、每个章节标题右端（9 枚）、每张自然专项卡（各 1 枚），
  静止时缓慢浮动；封面另加 5 颗珠光星点。
- 封面渐变改为粉→桃粉→紫→蓝，卡片圆角放大到 16-18px，描边偏移阴影保持贴纸感。

### 修正

- 依据索引表"编号 / 状态 / 取数日期"三列在窄列下被竖着切开，已加 nowrap + 窄屏降级。
- 待办勾选框边框太淡，加深到 `#c4b7d5`。

### 新增文档

- `references/design-system.md` 全量重写到 v3.0.0：色彩扩到六色循环、圆体排版规则、
  自然专项整行布局、贴纸清单与放置位置、布灵布灵四层背景、交互对照表。

---

## v4.1.0（2026-09-22）

视觉重做 + 修掉两个会让渲染直接失败的 bug。

### 修复

- **渲染器崩溃**（P0）：样式表抽到 `assets/` 之后，HTML 模板里的 `{CSS}` 变量名没跟着改，
  渲染时抛 `NameError`。`write_outputs` / `--check` 全线不可用，已修。
- **样式表被静默丢弃**（P0）：`load_css()` 用裸字符串匹配 `@import`，
  结果连注释里提到这个词都会被判定"含远程引用"，于是一直退回内置兜底样式，
  所有场景动效其实从未生效。改成**先剥注释再判断**，并只认真实远程地址。
- `--check` 的离线校验同步改为注释感知，避免正文/注释里的关键字误伤。

### 变更

- **交付物去掉「红方复核记录」**：复核是生成过程里的质量门，属过程记录，不该出现在攻略里。
  复核中未解决、读者必须知道的风险改写进「踩坑指南」。章节由 10 节变 **9 节**。
- **红方复核误报修正**：星期校验按星期序号比对（原来「周五（中秋节）」会被判不符）；
  满月夜观星规则改为在行程条目与自然专项里都查豁免说明。

### 视觉

- 全站改为 **B 站配色 × 描边贴纸**：弹幕粉 `#fb7299` + 番剧蓝 `#00a1d6`，
  中性色统一往蓝偏，不用纯黑纯白。
- **去掉纸张纹理背景**，改用渐变画的斜纹带 + 圆点阵列。
- 描边体系：1.5px 实线 + 同色系**实心偏移**（贴纸感），替换原来的柔光投影。
- 9 个章节各配一块描边小图标（内联 SVG，跟随 `currentColor`，奇偶节换色）。
- 封面新增**弹幕条**：把方案里最该先看到的事实（出发地→目的地、天数、日出、干潮、月相）
  横向飘一遍，只用真实数据，拿不到就不飘。
- 动效统一 `ease-out-quart`；四个自然专项主题动效保留并重新配色。

### UE

- 新增顶部**吸顶导航**（9 个锚点 + 返回顶部），`:target` 用粉底标出落点。
- 来源清单改为 `<details>` 默认折叠，需要核对时点开。
- 新增 `references/design-system.md` 完整重写：设计上下文、色板、排版、
  布局、图形语言、动效、信息架构、交互、可访问性、打印、自检清单。

---

## v4.0.0（2026-09-22）

交付前多了一道门：红方复核。同时把"数据要有出处"从原则变成了会被检查的规则。

### 新增

- **红方复核机制**
  - `scripts/red_team.py`：23 条规则的挑刺引擎，覆盖结构、预算、溯源、安全、逻辑五类
    - 安全类规则：赶海窗口必须盖住干潮（RT-L01）、窗口不得拖过干潮 1.5 小时（RT-L04）
    - 光线类规则：日出活动时刻与天文时刻比对（RT-L02）、满月夜排观星报警（RT-L03）
    - 支持 `check` / `rules` 子命令，有阻断项时退出码为 1
  - `references/red-team-checklist.md`：六轮质疑清单（时间/地理/成本/安全/事实/体感）
  - 复核结论写回 `trip.json` 的 `red_team` 字段，并在交付物中呈现
- **反幻觉溯源**
  - `facts` / `sources` 强绑定；结论状态四档 `verified / estimated / unverified / unknown`
  - 时效性自检：`api` / `official_site` / `web_search` 类来源超 30 天报警（RT-F04）
  - `unknown` 却填了数值直接报严重（RT-F05）
  - 交付物新增「依据索引」章节，列出每条结论的来源与取数日期
- **去 AI 味写作规范** `references/writing-style.md`：禁用词表、句式禁令、各模块写法、自检三问
- **交付渲染重做** `render_plan.py` v2.0.0
  - 手账风 UI：渐变封面、速览卡片、时间轴行程、专项卡、预算区间条、踩坑警示卡
  - 章节顺序固定为：速览 → 天气 → 自然专项 → 逐日行程 → 美食 → 预算 → 待办 → 踩坑 → 红方记录 → 依据索引
  - 新增打印样式；离线校验改为只拦"远程资源加载"，正文里的来源网址不算依赖
- **项目结构**：源码目录与安装目录分离，新增 `examples/`、`tests/`、`tools/install.py`
- **回归测试** `tests/run_tests.py`：24 条断言，含故意写坏的对照组

### 变更

- `SKILL.md` 主流程加入 S6.5 红方复核；数据诚实原则扩充为 11 条
- `render_plan.py --check` 改为精确的离线校验，不再误伤正文中的来源 URL

### 修正

- `red_team.py` 的"关键字→时刻"解析支持两种语序（`干潮 09:12` 与 `09:12 干潮`）
- 溯源关键词收紧，避免"10 月"被当成"月相"匹配

---

## v3.3.0（2026-09-22）

两个实时接口失效，全部换成可用方案。

### 修正

- **潮汐**：`cnss.com.cn` 的 tideJson 路径全部 404 → 改用 **Open-Meteo Marine API**，
  含干潮/满潮极值提取、三点抛物线插值（精到分钟）、赶海窗口计算、数据完整性标注
- **火烧云**：`sunsetbot.top` 域名已下线（HTTPS 525 / HTTP 404）→ 改用
  **Open-Meteo 自建评分模型**（云量分层 + 能见度 + 湿度 + 气溶胶，0-3 级）
- **参数注入**：`--city` 直接拼进 URL，加白名单正则校验

### 新增

- 城市名自动解析（Open-Meteo Geocoding），免手填坐标
- `selftest` 子命令，一键检测所有接口可用性

### 已知坑（写进脚本与文档）

- Open-Meteo Marine 潮位数据实际有效期约 10 天。请求 16 天时 `time` 数组照样返回 16 天，
  但第 10 天后全是 `null`。脚本会标 `partial: true` 并降级提示，不给出误导性窗口
- 潮位基准是 MSL，与官方潮汐表可能有系统偏差，安全决策须对照官方

---

## v3.2.0

重构为完全自包含版本：主流程 S1-S10 全编排 + 自然观察专项按需触发，不依赖任何其他 Skill。

- 五大自然专项：观鸟、观星、日出、赶海、朝晚霞
- 交付由内置的 `render_plan.py` 完成，同源渲染 MD + 单文件离线 HTML
