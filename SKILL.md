---
name: travel-orchestrator
description: 旅行规划总编排（完全自包含，含自然观察专项 + 红方复核）。当用户需要一次完整旅行方案，且需求涉及行程规划、观鸟/观星/日出/赶海等自然观察专项、美食推荐、预算规划、旅行灵感、待办提醒、踩坑指南等多项能力时调用。本 skill 是完整旅行规划的**主干与总设计师**，**不依赖任何其他 skill**：以自己的完整工作流（需求澄清→灵感→调研→天气→自然专项→行程→核查→**红方复核**→交付→出发前复核）驱动全程，内置观鸟（eBird 实时鸟况+识别）、观星（月相/行星/光污染）、日出日落（天文 API 精确时刻）、赶海（潮汐预报）四大自然专项与实时数据接口，内置**红方复核机制**（交付前由质疑方逐项挑刺，含可执行的 red_team.py 规则引擎）、**反幻觉溯源**（每条结论绑定来源与状态，未溯源不得写成确定值）、内置校验与 MD+HTML 同源渲染交付（render_plan.py），最终输出一份完整、可执行、基于实时数据校验的旅行方案（Markdown 攻略 + 单文件离线 HTML 攻略页）。适用于国内/出境自由行、亲子游、自驾游、周末短途等场景的"要一份全面旅行方案"类请求。
display_name: 旅行规划总编排
display_name_en: Travel Orchestrator
description_zh: "带红方复核的旅行规划：先把潮汐、日出、月落这些改不了的时刻锁死，再倒推整条动线。开场是逐层访谈而不是问卷，自然观察专项（观鸟 / 观星 / 日出 / 赶海）先做可行性核查再决定排不排，交付前由质疑方逐项挑刺，每个数字都能追到来源。产出同源的 Markdown 攻略与单文件离线 HTML 攻略页。"
description_en: "Travel planning with an adversarial review pass: lock the moments that cannot move (tide, sunrise, moonset), then work the route backwards. Opens with a layered interview instead of a form, checks feasibility before scheduling any nature activity, and runs an adversarial pass before delivery. Every number traces back to a source. Ships a Markdown guide plus a single-file offline HTML page."
version: 4.6.0
author: travel-orchestrator
agent_created: true
---

# Travel Orchestrator：旅行规划总编排（完全自包含）

把一次旅行需求从「想去」变成「一份能直接出发的完整方案」。**本 skill 完全自包含，不依赖任何其他 skill**，全程以自己的流程、脚本和数据接口驱动：

- **自包含**：需求澄清 → 灵感推荐 → 实时调研 → 天气研判 → 自然观察专项 → 行程编排 → 核查 → 待办/踩坑 → 成品交付 → 出发前复核，全流程由本 skill 内置完成
- **差异化核心**：观鸟 / 观星 / 日出 / 赶海 四大自然专项 + 实时数据接口（eBird、潮汐、月相、日出日落、火烧云），这是通用旅行 skill 都没有的
- **内置交付**：`scripts/render_plan.py` 从同一份行程数据同源渲染 Markdown 攻略 + 单文件离线 HTML 攻略页，无需任何外部脚本

## 数据诚实原则（本 skill 的底层准则）

1. **不为完整而编造数据**：未知就是 `unknown`/`null`，不是 0、不是"正常开放"；查不到的价格/时间如实写未知
2. **每条结论都要有出处（反幻觉核心）**：方案里的时刻、价格、里程、鸟种、潮位等具体数字，
   必须能追到 `sources` 里的某一条；追不到的一律降级标注，**不许以确定语气写出来**
3. **先沟通后规划**：方向性信息（出发地/时间/预算口径）缺失时不假装"量身定制"，用明确标注的假设交付探索草案
4. **单一业务数据源**：行程数据只有一份，以**分片形式**存（每片几 KB，写不坏）：分片多时收进
   `travel-plan/<trip_id>/parts/*.json`，少时直接平铺在 `travel-plan/<trip_id>/`，两种形态 `resolve_trip` 都认。
   渲染、红方复核、校验都**直接读目录、就地合并**，不落中间大文件，这一步同时消灭了「组装写坏」和「产物与分片不同步」两个故障源。
   确实需要一份独立大 JSON（交付/存档）时才 `assemble` 或 `render --write-json` 生成，生成物不是数据源
5. **同源双输出**：Markdown 攻略与单文件 HTML 攻略页从同一份数据渲染，保证事实一致
6. **先核查后交付，交付前必过红方**：结构+时间+预算+溯源检查完成后，再由红方逐项挑刺；
   blocking 未清零不得标为可执行，方案状态如实标注（draft/conditional/executable_as_of_check）
7. **版本存档**：每次调整保留旧版本（整目录快照 `versions/vN/`，或 `versions/trip.vN.json`），可回退、可比较
8. **不增加注册门槛**：优先用免费 API 与宿主已有工具；eBird Token 可选（无 Token 也能完成全部基础流程）
9. **署名与透明**：交付标注来源+日期，让用户知道哪些已核查、哪些是估算、哪些没查到
10. **时效性自检**：潮汐/日出/月相/天气四类数据一天一变，来源超过 30 天即视为过期，必须重新取数
11. **文案去 AI 味**：交付文案按 `references/writing-style.md` 写，具体优先、口令化、不写空话（详见"交付规范"）

### 结论状态四档（写进 `facts` 分片）

| 状态 | 含义 | 能否写进正文当确定值 |
|---|---|---|
| `verified` | 有权威来源且已核对 | 可以 |
| `estimated` | 有依据的推算或部分数据 | 可以，但必须标注"估算/部分数据" |
| `unverified` | 有说法但没核对 | 可以，但必须标注"未核实" |
| `unknown` | 查不到 | 不可以，正文写"未查到"，`value` 留空 |

## 能力来源参考（哪些借鉴、哪些自研）

本 skill 在开发时参考了三个现有旅行 Skill 的实现，取其方法论精华，但**不依赖、不调用它们**，全部能力已内置：

| 参考项目 | 借鉴的能力 | 本 skill 的自研/增强 |
|---|---|---|
| bys-travel-plan（不一书，MIT） | 数据诚实原则（未知=null）、方案三档状态、单一数据源、同源双输出、版本存档、预约状态管理 | 完全自研的 `render_plan.py` 渲染器 + 简化数据结构 + 专属校验流程 |
| travel-planner-wb（旅游规划） | 实时搜索纪律（禁凭记忆写价格）、双时点天气、预算六分项、避坑清单 | 独立的调研/天气/预算步骤设计 + 专项对表编排 |
| travel-planner-v2（AI 旅行规划师） | "帮我想/帮我订/帮我办"三段式思想、质检防坑 | 四阶段闭环重构 + 自然专项融合 + 内置交付 |

**核心差异化（本 skill 独有）**：观鸟 / 观星 / 日出 / 赶海 四大自然专项（火烧云评分并入日出日落卡）+ 实时数据接口（eBird、CycleCalcs、Sunrise-Sunset、Open-Meteo Marine），按需触发，通用旅行 skill 均不具备。

## 触发与路由

触发场景（满足任一即可）：

- 「帮我规划一趟旅行 / 做攻略 / 安排行程」+ 提到观鸟、观星、日出、日落、赶海、摄影、自然观察等专项需求
- 用户要求"完整旅行方案"，需要行程 + 美食 + 预算 + 清单等多模块产出
- 用户给了目的地/天数/预算但还想要灵感推荐、待办提醒、踩坑指南
- 用户说"初始化/使用 旅行规划总编排"

不适用：单纯查机票酒店价格（直接提示官方查询渠道）、单纯景点问答（直接回答）。

## 核心工作流

按以下 10 模块推进，全部由本 skill 内置完成。**主流程全编排；S1 是逐层访谈而不是一次抛问卷，自然观察专项由 S1 的 L3 环节确认后才展开**：

| 模块 | 说明 |
|---|---|
| S1 需求澄清（访谈） | 一次一问、逐层拷问需求（意图→硬约束→口味→自然专项→底线），写出「需求共识」并等用户确认；协议见 `references/intake-interview.md` |
| S2 灵感与目的地推荐 | 无目的地时推荐 3-5 个候选并比较取舍，叠加专项偏好 |
| S3 实时调研（门票/预约/交通/政策） | 建调研清单、记事实与来源，标注日期 |
| S4 天气研判 | 规划期气候 + 出发前逐日复核 |
| S5 行程编排 + 美食推荐 + 预算 | 八步规划，专项时点先锁定，六分项预算 |
| S6 核查与修正 | 结构+时间+预算+溯源检查，方案状态如实标注 |
| S6.5 红方复核 ⭐ | 交付前由质疑方逐项挑刺（安全/逻辑/事实/成本/体感），blocking 清零才放行 |
| S7 待办提醒 + 踩坑指南 | 预约/证件/行前清单 + 专项清单 |
| S8 成品交付 | `render_plan.py` 同源渲染 MD + HTML 攻略页（B 站配色、九节、可点可折叠），过一遍文案与一致性检查 |
| S9 出发前复核 | 重查过期信息、潮汐/霞光/观星条件复查 |
| S10 修改与版本存档 | 保留旧版本，局部更新，可回退比较 |
| ⭐ 自然观察专项（按需） | 观鸟 / 观星 / 日出 / 赶海（含火烧云评分）由 S1 的 L3 环节确认后才展开，见下节 |

### S1 需求澄清（访谈，不是问卷）

**借 grill-me 的拷问式访谈把需求问清楚：一次只问一个问题（用 `AskUserQuestion` 给 2-4 个具体选项、
推荐项排第一并标「推荐」），等用户答完再问下一个；能自己查到的事实（天气/潮汐/月相/票价/车次）
自己查，把查到的事实嵌进问题里再问；共识确认前不给任何方案。**
完整协议（五层决策树、追问对照表、收尾模板、反例清单）见 `references/intake-interview.md`。

五层顺序推进，每层的答案决定下一层问什么：

| 层 | 问什么 | 落盘 |
|---|---|---|
| L0 意图 | 这趟为谁、为了什么、最不想发生什么 | `request.why` / `anti_goals[]` |
| L1 硬约束 | 日期窗（固定还是可挪）、同行人**结构**、预算口径（总额/人均、含不含大交通）、出发地、证件与体力 | `request.origin/dates/companions/budget/constraints[]` |
| L2 玩法口味 | 节奏、作息（能不能早起直接决定专项能不能排）、钱花在哪 | `request.pace` / `taste` |
| L3 自然观察专项 | **先查可行性，再问要不要玩**；可行逐项深挖，做不到直说并给替代 | `request.interests` / `nature_caps` |
| L4 风险与底线 | 安全底线、天气备选、不确定性容忍、忌讳 | `request.risk` |

- 正常 **8-12 问**收敛，判断标准不是问满几轮，而是**能不能在不假设任何前提的情况下排出一张日程表**
- 用户说「你看着办 / 随便你安排」→ 走**快速通道**（只问 L0+L1），其余标 `default_suggested`，
  并在方案里明确列出"这几处是我替你定的"
- 含糊答案（"随便""都行""看情况"）不算答案，必须追一层到具体值（对照表见协议文档 §2）
- 发现矛盾**直接顶回去**并给替代选项（预算与档期不符、满月夜要拍银河、带 3 岁娃要凌晨赶海）
- 相对日期（"下个周末""国庆"）转成绝对日期后**让用户确认**，后续计算一律用绝对日期
- 每个被问到的决定在 `decisions[]` 留一条（`question`/`choice`/`reason`；
  被顶回过、用户仍坚持的加 `pushed_back` 说明）。**这是过程记录，与红方复核同理
  不渲染进交付物**；交付物只留共识的结论（`why` → 速览「这趟的主题」，
  `constraints[]` → 速览「硬约束」）
- **收尾必须输出「需求共识」块并等用户确认**，确认后才进 S2/S3；S3 调研若发现与共识冲突的事实，
  回头找用户改共识，不许自己偷偷改方案

### S2 灵感与目的地推荐

用户没定目的地时，由本 skill 主导目的地决策（粗筛 → 3-5 个有真实差异的候选 → 有解释的维度比较，不给貌似客观的总分），并叠加专项偏好：

- 观鸟：候鸟迁徙季（春秋）优先湿地、保护区（如鄱阳湖、东滩、若尔盖）
- 观星：避开光污染，优先高原/沙漠/国家级暗夜公园（如茶卡、川西、库布齐）
- 日出：云海日出（黄山/武功山/泰山）、海上日出（海边）、沙漠日出
- 赶海：优先海岸线平缓、滩涂宽阔的滨海城市（青岛、烟台、厦门、北海、秦皇岛）
- 每个候选给：匹配理由、往返时间、扣除交通后的实际可玩时间、预算范围、主要缺点、关键不确定性

### S3 实时调研

先建调研清单再查（web 搜索），每条重要结论记录：结论/来源/发布时间/适用日期/状态。**禁止凭模型记忆写景点和价格**，未知写 `unknown` 而非 0 或"正常开放"；关键信息标注来源与日期。

### S4 天气研判

双时点查询：
- 规划期：目的地该季节气候特征（影响室内外配比、穿衣建议）
- 出发前：逐日天气预报，用 emoji 展示（☀️☁️🌧️），标注对当日行程的影响（如"D2 有雨→改室内备选"）
- 写入 `itinerary.weather`（kind: seasonal/forecast + 来源）

### S5 行程编排 + 美食推荐 + 预算

八步规划（锁定安排 → 过滤候选 → 片区分天 → 排时间窗 → 插入体验 → 补全生活时间 → 局部改善 → 压力测试），叠加专项时点：

- **专项对表**：赶海锁死干潮前后 1.5 小时、观鸟/日出排清晨 6-8 点、观星排 21 点后，先锁定时点再排其他
- **美食推荐**：搜索目的地必吃/老字号/本地人推荐，结合同行人口味（辣/清淡/儿童餐），标注人均价位与是否需排队/预约；海边城市补充海鲜市场/赶海后加工建议
- **预算明细**：大交通/住宿/门票/市内交通/餐饮/其他 六分项，金额人民币，儿童老人优惠票单独算，预留 10% 弹性；分项入 `itinerary.budget`
- 每个主要景点给雨天/预约满备选（含触发条件/替换哪段/从哪接回/费用变化）

### S6 核查与修正（机器可判部分）

先用规则引擎把机械错误扫一遍，不要靠肉眼：

```bash
python scripts/red_team.py check travel-plan/<trip_id>   # 目录或 trip.json 都行
```

它覆盖 23 条规则：日期星期、ID 唯一、时间重叠、时间窗有效性、预算算术与超支、
结论是否溯源、来源是否过期、未知值是否被写成数字、赶海窗口与干潮是否对得上、
日出活动与天文时刻是否矛盾、满月夜排观星、凌晨与清晨专项间隔、备选是否缺失等
（`python scripts/red_team.py rules` 看全表）。

- 有 `blocking` 必须先改完再往下走，脚本退出码即为信号
- 语义层的问题（路线是否折返、证据是否支持结论）机器判不了，交给 S6.5
- 方案状态三档如实标注：`draft`（草案）/ `conditional`（有条件可用）/ `executable_as_of_check`（已核查可用）

### S6.5 红方复核（对抗式挑刺）⭐ 本 skill 的核心质量门

**规则引擎只能抓机械错误，抓不到"看着挺合理但其实不行"。所以交付前换一个立场再过一遍。**

红方的立场是**站在出事故的那一边想问题**：蓝方写"这样玩挺好"，红方问"这样玩哪里会崩"。

**执行方式**（两种，建议都做）：

1. **规则引擎**：`red_team.py check` 已覆盖可判定的部分（见 S6）
2. **红方 agent**：以质疑方身份按 `references/red-team-checklist.md` 的六轮清单复核
   （时间 → 地理 → 成本 → 安全 → 事实 → 体感），逐条产出带证据与替代动作的质疑

**硬性要求**：

- 每个质疑必须落到**具体某一行**，不能只说"行程太赶"
- 质疑必须给**替代动作**，只提问题不给解法等于甩锅
- **安全相关一律升为 `blocking`**：判断标准是"出错的后果是玩得不爽，还是人回不来"
- 复核结论写回 `red_team` 字段（对应 `parts/*_red_team.json` 分片，结构见 checklist 文档），
  含 `severity / status（resolved·accepted_risk·open）/ action`
- **`open_blocking` 必须为 0**，方案才能标 `executable_as_of_check`
- 一般两轮：第一轮挑问题，第二轮确认修改没引入新问题。第三轮还能挑出阻塞项 → 该重做方案
- 若某轮确实没发现，写 `"findings": []` 明确记录"本轮无发现"，不许省略整轮

**红方最常抓到的东西**（真实案例）：
用错日期的潮汐数据导致赶海窗口整个错位、预算分项上限合计超支、
写死的车次/票价其实没有依据、满月夜还排深空观星、
观星到 23:00 而次日 05:00 拍日出（中间只剩 6 小时含路上和洗漱）。

### S7 待办提醒 + 踩坑指南

- **待办提醒**：checklist（预约渠道/截止时间/未解决怎么办 六段式）+ 专项清单（观鸟设备、观星保暖、日出占位、赶海装备）
- **踩坑指南**：搜索中发现的消费陷阱、管制信息、高峰期时段、停车/排队建议、观鸟观星赶海常见坑（保护区禁入、观星点无信号、日出点停车罚款、赶海看错潮汐时间被海水困住，**这是最危险的坑，必须把潮汐表放进方案**）

### S8 成品交付（内置渲染器）

- `python scripts/build_trip.py render travel-plan/<trip_id>` → 合并分片 + 结构校验 + 渲染一条龙：
  - **Markdown 完整攻略**（.md）
  - **单文件 HTML 攻略页**（.html，双击可开、离线可用、无 CDN、无脚本、可直接打印）
- 只渲染也行：`python scripts/render_plan.py travel-plan/<trip_id>`（同样直接读目录，不要求先组装）
- `python scripts/render_plan.py <目录或 trip.json> --check` 确认产物一致性（存在、署名、无远程资源、关键数字未丢失）
- 🔴 **结构不过不出图**：渲染前按 build_trip 的规则先校验结构（缺字段 / item_id 重复 / 预算倒挂等），
  不通过直接拒绝渲染并返回退出码 3，不再出现「命令返回成功、交付物其实是废纸」。只有排障才用 `--force`
- **章节顺序固定**（渲染器已内置，不要自行打乱）：一页速览 → 天气研判 → 自然观察专项 →
  逐日行程 → 美食推荐 → 预算明细 → 出发前待办 → 踩坑指南 → 依据索引（共 9 节）
- **交付前确认门**：方案框架（目的地/天数/预算档位/节奏）先给用户确认，确认后再细化；用户赶时间可一次交付并注明可调整点
- **过程记录不进交付物**：红方复核、S1 访谈的问答流水（`decisions[]`）、字段的来源状态标记都属于生成过程
  - 红方复核中仍未解决、读者必须知道的风险，**改写进「踩坑指南」**，不要以「复核记录」的形式出现在攻略里
  - 访谈的结论要留：`request.why`（速览「这趟的主题」）与 `request.constraints[]`（速览「硬约束」），
    但一问一答的过程不渲染：读者要的是行程，不是问答流水

### S9 出发前复核

方案交付后（或用户临出发时）执行一次复核：
- 天气逐日预报重查
- 潮汐表更新（临近日期更准）
- 霞光/观星条件复查（`nature_forecast.py` 重跑）
- 门票预约确认、交通时刻确认
- 输出「出发前核对清单」，把与方案不符的变化点标红提醒

### S10 修改与版本存档

用户要求调整方案时：
- 先整目录快照到 `versions/vN/`（或 `versions/trip.vN.json`），再修改
- 按影响范围局部更新（改某天→同步交通/预算/清单/备用），不重做整次旅行
- 修改后重跑核查 + 重新渲染 MD/HTML，保留旧版本可回退
- 新对话恢复：读当前环境真实存在的 `travel-plan/<trip_id>/` 存档；读不到就请用户重新提供已生成文件

## 自然观察专项（⭐ 按需触发模块）

**触发规则**：专项是 **S1 访谈 L3 的一个环节，不是单独的问卷**。拿到队里的人和日期之后**先跑一遍可行性核查**
（`nature_forecast.py`），再按三档如实告诉用户、让他自己选：条件好 → 问做不做；
条件一般（月照强、潮位一般）→ 说清代价再问值不值得专门排；**做不了 → 直说做不了并给替代**。
用户不感兴趣的项直接跳过，不影响主流程。展开后优先调实时数据接口（`references/nature-apis.md`
+ `scripts/nature_forecast.py`），结论以事实记录并入 `facts` 分片。

⛔ **不许为了"满足用户"硬排一个玩不到的项目**：目标鸟种不在该季节、观测目标当天在地平线下、
那天没有潮汐窗口、根本不是流星雨季：这些都要在访谈里说清，别等用户到了当地才发现。

**专项一：观鸟指导（实时鸟况 + 识别 + 知识库）**
- **本地鸟种清单（最硬的数据）**：调用 `python scripts/nature_forecast.py bird --region CN-XX --token ...`（eBird API）查目的地近 7-30 天**近期观测**，得到当前实际可见鸟种（含中文名）；无 Token 时提示用户在 https://ebird.org/api/keygen 免费申请（~1000 次/天，非商业免费），或先用 web 搜索降级
- **观鸟热点**：`birdspot` 功能查目的地附近 25km 的 eBird hotspot，优先推荐有近期记录的
- **识别鸟**：声音用 BirdNET（康奈尔开源，`pip install birdnet`，6000+ 物种；无录音时可用 `predict(纬度, 经度, week=第几周)` 预测该地该周可能物种）；照片按体型/嘴型/羽色/行为/鸣声特征鉴别，中文名以《中国鸟类野外手册》为准
- 结合 `references/nature-knowledge.md` 的鸟种季节表给专业建议，标注最佳时段（清晨 5-8 点）与注意事项（不惊鸟、不投喂、穿素色衣）

**专项二：观星指导（调 CycleCalcs API 精准计算）**
- 调用 `python scripts/nature_forecast.py astro --lat .. --lng ..`（cyclecalcs.com 免费 API，无需 Key）：
  - 月相与月亮照度（避开满月）、行星可见性（金木水火土哪些在天上）
  - 暗夜窗口（天文昏影终→天文晨光始）、流星雨日历与近日事件（如 8 月英仙座、12 月双子座）
- 查目的地光污染等级（lightpollutionmap.info 或波特尔分级），选暗夜观测点
- 海拔与天气：高海拔 + 晴天 + 低湿度为佳；提醒保暖与氧气（高原）
- 输出：逐日观星评分（月相 + 云量 + 光污染综合，1-5 星），推荐观测时间窗

**专项三：日出日落指导（调天文 API 精确到分钟）**
- 调用 `python scripts/nature_forecast.py sun --lat .. --lng .. --date ..`（api.sunrise-sunset.org 免费 API）：精确日出/日落时刻、黄金时刻/蓝调时刻、晨昏蒙影时间
- 最佳观景点方位（东/西向视野）、云海日出概率（结合天气：前一天降水 + 次日晨晴）
- 是否需要提前占位（旺季热门日出点）、往返交通与头班车时间
- 摄影建议：三脚架、长曝光、渐变滤镜；提醒日出前 30 分钟到达

**专项四：赶海指导（调潮汐 API 精准预测）**
- 调用 `python scripts/nature_forecast.py tide --city .. --date ..`（Open-Meteo Marine API，免费无 Key）：
  - 逐日满潮/干潮（低潮）时刻与潮高（局部极值 + 三点抛物线插值，精确到分钟）
  - **推荐赶海窗口**：干潮前后 1.5 小时（潮水最低时滩涂裸露最广）
  - 半日潮区域每天通常 2 干 2 满；农历初一/十五前后大潮，退得远赶海收获多
  - ⚠️ 潮位为 MSL 基准且数据有效期约 10 天；目标日期数据不完整时脚本会标 `partial`，
    **此时必须对照官方潮汐表复核，不得直接采信**
- 赶海地点推荐：滩涂宽阔、沙质细腻、有礁石区的海岸（参考 nature-knowledge.md §4.5）
- 安全提醒：涨潮速度、暗沟、带小孩特别注意潮水回流；查询当天是否有台风/大风
- 装备清单：雨靴、手套、小铲子、盐瓶（抓蛏子）、水桶

**专项四的补充：火烧云 / 朝晚霞预报（并入日出日落卡）**
- 调用 `python scripts/nature_forecast.py glow --city .. --event set|rise`（Open-Meteo 自建评分模型，免费无 Key）
  - 输出 0-3 级霞光评分（无 / 一般 / 较好 / 绝佳）+ 逐因子判断理由
  - 依据：中高云适中（20-75%）+ 低空通透 + 水汽适中 + 适度气溶胶；雨后初晴傍晚概率大
  - ⚠️ sunsetbot.top 已下线；本模型为**参考值非官方预报**，交付时必须如实标注，建议用户结合实景判断

**专项与行程融合**：赶海锁死干潮前后 1.5 小时、观鸟/日出排清晨 6-8 点、观星排 21 点后，先锁定时点再排其他；每个专项给雨天备选；观星点/赶海点远的提示回程安全。

**专项的交互件（渲染层，写在 `scripts/nature_widgets.py`）**：攻略页上的自然专项不只是文字，四项各有一个能点的东西，全部纯 CSS 无脚本：

| 专项 | 交互件 | 需要给到的字段 |
|---|---|---|
| 观鸟 | 鸟种卡：点开看学名/科属/保护等级/简介，并给图鉴与查图外链 | `species[]` 写成对象（`sci`/`family`/`status[]`/`intro`/`intro_source`/`best_months`/`taxa_url`），写字符串只能降级成小胶囊且必须只写鸟名 |
| 观星 | 示意星图（真算地平坐标）+ 月亮相位盘 + 「今晚看什么」 | `chart{lat,lng,tz,at,place}`；`targets[]`；`spots` |
| 日出日落 | 天光条（蓝调/黄金/白天/夜一条横轴）+ 霞光指数点阵 | `sunrise`/`sunset`/`golden_hour`/`blue_hour`；霞光写进 `nature.glow`（`score`/`level`/`reasons`/`inputs`） |
| 赶海 | 潮汐图点选：点曲线上的圆点，换该时刻潮位与下滩建议 | `curve[{t,h}]`（接口逐时潮位）+ `extremes[{type,time,height_m}]` + `best_window`/`unit`/`source_id` |

三条要求：
- **地点字段**（`hotspots`/`spots`/`best_spot`）写成 `[{name, note}]` 或 `["A","B"]`，渲染时会变成可点的高德搜索链接；写成一句话也能用，但会拿整句话去搜
- **`species[]` 的纯字符串只写鸟名**（`"大天鹅"`），说明写进 `nature.birding.notes`。
  渲染层会把长句按首个标点拆成「鸟名 + 说明」，但拆出来的鸟名未必对，别指望它兜底。
- 潮汐图**只画接口真实给到的时段**，没数据的时段不补线（威海的例子就只到 07:00）；星图必须写明是「按 X 坐标 Y 时刻算的示意星图，未做大气折射与光污染修正」，不要写成精密星历

## 交付规范

### 章节顺序（固定）

一页速览 → 天气研判 → 自然观察专项 → 逐日行程 → 美食推荐 → 预算明细 →
出发前待办 → 踩坑指南 → 依据索引（共 9 节）

顺序的逻辑：**先让用户 30 秒看懂全局，再给可执行细节，最后交底（依据）。**
不宜把"使用须知"放最前面长篇声明，一句带过即可；不宜把来源堆到开头，那是交底不是开场。

### 文案（必须过去 AI 味）

按 `references/writing-style.md` 执行。核心三条：

1. **删掉这句，用户会少知道什么？** 答不上来就删
2. **这句话里的数字，追得到来源吗？** 追不到就标估算或改 `unknown`
3. **念出来像人在说话吗？** 像报告就重写

禁用"值得一提的是/首先其次最后/总的来说/不仅而且/打造/赋能/致力于/绝美/不容错过"等词，
emoji 只用于分类标记不用于强调语气。

### 事实与溯源

- 方案开头一句「使用须知」：时间/价格/预约以官方为准，出发前复核
- 关键信息标注来源 + 日期；未知写"未查到"，不写 0 或"正常开放"
- **结尾必须有「依据索引」**：列出 `facts`（结论 + 状态 + 来源 + 取数日期）与 `sources`（名称/类型/URL/取数日期）
- 自然观察专项独立成节，含最佳时间、地点、设备、注意事项 + 实时数据
- MD 与 HTML 同源同 `plan_version`，保证事实一致
- 所有产物本地落盘（`travel-plan/<trip_id>/`），交付时给出文件路径

## 注意事项

- 🔴 **AI 生成内容必须人工核对**：时间/价格/预约/潮汐/月相信息以官方或数据源为准
- 🔴 潮汐、日出时刻、月相属**天文/海洋预报**，接口返回后仍需对照官方来源复核（如国家海洋信息中心、天文台）
- 🔴 **日出日落/月相/霞光这类时刻敏感的数据一律用 `--lat/--lng` 取，不要用 `--city`。**
  中文重名地名极多，`--city 大同` 会解析到黑龙江大同（46.04N 124.81E），与山西大同的
  日出差 46 分钟，而脚本不会报错。`nature_forecast.py` v2.3.0 起会在同名候选跨省时
  打一行 `[注意]`，但取数仍取第一条；不得不用城市名时先核对输出里的 `admin1`，
  坐标按**实际观测点**取（观星点离市区几十公里时尤其重要）。详见 `references/nature-apis.md` §6.2
- 🔴 个人出行信息只本地交付，不发公开群；eBird Token 只走环境变量，⛔ 不进行程数据/MD/HTML
- 观鸟观星赶海需实时信息（鸟况、天气、潮汐、月相）时，用接口 + web 搜索最新数据，不要凭记忆
- 参数不足时先问清关键项（出发地/日期/人数/专项偏好），不要瞎猜硬编
- 若用户是出境游，额外提醒签证/时差/插头/网络等事项
- 接口失败（超时/403/空结果/404/SSL）时不阻塞：改用 web 搜索对应数据，并标注数据源与日期
- 涉及自然专项的方案，交付前可跑 `python scripts/nature_forecast.py selftest` 确认接口健康度；
  接口失效属正常现象（本 skill 已有两个接口因对方下线被替换），发现失效须同步更新 `references/nature-apis.md`
- 数据不完整时**必须显式标注**（如潮汐 `partial: true`），⛔ 不得静默输出残缺数据冒充完整结果
- 🔴 **交付前必须跑一遍红方**：`python scripts/red_team.py check <方案目录>`；
  存在 `blocking` 时不得交付，`major` 未处理的须在方案中标为已知取舍
- 🔴 **每个具体数字都要能追到 `sources`**：追不到就标"估算/未核实"，或直接写"未查到"；
  ⛔ 不许用"约""左右""大约"把没有依据的数字包装得像有依据
- 交付文案写完对照 `references/writing-style.md` 的自检三问过一遍，过了再交付

## Resources

### references/
- `intake-interview.md`：**需求访谈协议（S1 执行手册）**，四条硬规则（一次一问 / 带推荐项 / 能查的不问 / 共识前不写方案）、
  五层决策树、自然专项的可行性三档问法、追问对照表、收尾「需求共识」模板与落盘字段映射、反例清单
- `nature-knowledge.md`：观鸟/观星/日出/赶海专业知识库：鸟种季节表、波特尔暗空分级、月相与银河可见性、日出日落时刻名词、赶海物种与滩涂类型、专项与行程融合原则
- `nature-apis.md`：实时数据接口清单与调用示例（潮汐 Open-Meteo Marine、观星 CycleCalcs、日出日落、eBird 观鸟、火烧云自建评分模型、地理编码），含**接口可用性状态表**、算法说明、统一降级规则与「已停用接口」清单
- `red-team-checklist.md`：**红方评审清单**，质疑纪律、严重度定义、六轮质疑（时间/地理/成本/安全/事实/体感）、高频陷阱速查、`red_team` 字段输出格式
- `writing-style.md`：**去 AI 味写作规范**，禁用词表、句式禁令、各模块写法、交付前自检三问、改前改后对照

### scripts/
- `build_trip.py`：**行程数据分片工具** v1.1.0：`skeleton` 生成分片骨架、`render` 读目录合并+校验+渲染一条龙、
  `split` 把已有大 JSON 拆成分片（自检等值）、`validate` 校验（目录或文件都行）、`assemble` 组装独立 JSON、
  `schema` 打印最小结构。**数据以分片为常态，不落中间大文件**
- `nature_forecast.py`：自然专项实时数据查询脚本 v2.3.0（潮汐 / 日出日落 / 观星 / 火烧云 / 观鸟 / 物种卡 / 城市解析），
  全部走免费无 Key 接口（eBird 为可选增强，需免费 Token），失败自动降级提示；`selftest` 子命令一键检测所有接口可用性
- `nature_widgets.py`：自然专项交互件 v1.1.0：鸟种卡、示意星图（内置亮星表 + 地平坐标换算）、
  天光条与霞光指数、潮汐点选图。输出内联 SVG/HTML，无脚本、无远程资源
- `render_plan.py`：交付渲染器 v3.4.0：从行程数据（目录内 parts/ 或独立 trip.json）同源渲染 Markdown 攻略 + 单文件离线 HTML 攻略页
  （含 B 站配色封面、九节正文、自然专项四种交互件、可折叠逐日行程、可点地图链接与来源链接、依据索引、打印样式），
  含 `--check` 一致性检查
- `red_team.py`：**红方复核规则引擎** v1.2.0：23 条规则覆盖结构、预算、溯源、安全与逻辑五类，
  输出分级 finding 与结论；`check <方案目录或 trip.json>` 复核、`rules` 列出全表；存在阻断项时退出码为 1

### assets/
- `handbook.css`：交付页样式源（渲染时内联进 HTML）。改样式改这个文件，不要把 CSS 塞进 render_plan.py

### examples/ 与 tests/（随 skill 分发）
- `examples/weihai-2d/`：开箱示例：13 个分片（`01_request.json` … `27_decisions.json`，含 facts/sources/red_team
  与访谈拍板记录）+ 渲染出的 MD + HTML。分片在这里是平铺的（上架包限两层目录），真实方案更推荐收进 `parts/`
- `tests/run_tests.py`：规则引擎与渲染器的回归测试（含故意写坏的对照组，验证阻断项能被正确抓出）

---

*v4.6.0 | travel-orchestrator 完全自包含版：主流程 S1-S10 全编排 + S6.5 红方复核（对抗式挑刺）+
自然观察专项按需触发（观鸟 / 观星 / 日出 / 赶海，含火烧云评分）+ 反幻觉溯源 + 去 AI 味交付，不依赖任何其他 skill*

*v4.6.0 生成环节优化：**数据以分片为常态，不再落中间大文件**。① 行程数据固定在 `parts/*.json`
（示例已按此形态落地，13 片、最大 3.4 KB），渲染 / 红方复核 / 校验**直接读目录就地合并**，
`build_trip.py` 新增 `render` 与 `split`（拆分后自动校验等值），`assemble` 降级为「需要独立大 JSON 时才用」；
② 渲染器前面补**结构门**：结构不通过拒绝渲染、退出码 3，坏数据不再"返回成功但交付物是废纸"（`--force` 仅排障）；
③ 新增用例 9 守住这三件事；④ 验收渲染改出到**临时目录**（原先就地重渲染示例产物，
而产物带「生成于」时间戳，每跑一次验收就把仓库弄脏一次），打包产物改落工作区 `.workbuddy/dist/`
而不进源码目录，新增用例 10 看住这两点，断言 129 → 133 条。*

*v4.5.2 补回大同那轮的三个修复（原修复只落在安装目录、被同步覆盖，这次进源码并加断言看住）：
  ① `nature_forecast.py` v2.3.0 地理编码改 `count=5`，同名候选跨省时打 `[注意]`（大同 → 黑龙江的坑）；
  ② MD「使用须知」不再硬编码「赶海潮汐」，改为跟着方案实际有的专项走；
  ③ 鸟种卡的降级胶囊不再被一句话撑坏，外链只拿鸟名去搜。
  文档补回 `references/nature-apis.md` §6.2 与 SKILL.md 硬规则；断言 112 条。*

*v4.5.1 上架前收尾：① `LICENSE` 著作权人改为项目名 `travel-orchestrator`；
  ② frontmatter 补齐开放平台必填项（`description_zh` / `description_en` / `version` / `author`，
  外加展示名两行），`author` 与 `LICENSE` 同口径；③ 新增用例 7 守住这些元数据，77 → 91 条断言；
  ④ 页脚版本号改为 v4.5.1。功能与文案未动。示例样本暂缓补入，先用真实行程回填。*

*v4.5.0 文档对齐（无功能改动）：① 断言数与脚本版本号补正（77 条断言、`render_plan.py` v3.2.0、
  `red_team.py` v1.1.0、`nature_widgets.py` v1.0.1）；② 自然专项统一为**四项**，火烧云评分并入日出日落卡；
  ③ 赶海窗口口径统一为**干潮前后 1.5 小时**（原来 SKILL.md 有两处写 2 小时，与 `nature_forecast.py` 不符）；
  ④ 全库正文清掉中文破折号 221 处、补齐中英混排空格；⑤ 新增 `workbuddy.json`（展示名与中英简介）。*

*v4.4.0 新增：① S1 由"一次抛问卷"改为 **grill-me 式需求访谈**（一次一问、逐层拷问、
  能查的不问用户、共识确认前不写方案），协议见 `references/intake-interview.md`；
  ② 自然观察专项**嵌进访谈 L3**：先做可行性核查再问，做不了的项目直说并给替代；
  ③ 交付物「一页速览」显示这趟的主题（`request.why`）与硬约束（`request.constraints`）；
  一问一答的 **`decisions[]` 属过程记录，不进交付物**（与红方复核同理）。*

*v4.0.0 新增：① 红方复核机制（`red_team.py` 23 条规则 + `red-team-checklist.md` 六轮质疑清单），
交付前 blocking 清零才放行；② 反幻觉溯源（facts/sources 强绑定、结论状态四档、时效性 30 天自检、
"依据索引"章节）；③ 交付渲染重做（B 站配色 UI、固定章节顺序、打印样式）；④ 去 AI 味写作规范；
⑤ 本地项目目录结构（源码目录与安装目录分离，含 examples/tests）。*

*v3.3.0 接口修复：① 潮汐由已失效的 cnss.com.cn 改用 Open-Meteo Marine API；② 火烧云由已下线的 sunsetbot.top
改用 Open-Meteo 自建评分模型；③ 新增城市名解析与参数白名单校验；④ 新增 `selftest` 一键接口自检。*
